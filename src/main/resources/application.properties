spring.application.name=duo
server.port=${SERVER_PORT:8081}

# Configure these when you want /api/furniture to read from MySQL.
# Without them, the app starts normally and serves the bundled furniture.csv catalog.
# SPRING_DATASOURCE_URL=jdbc:mysql://localhost:3306/Duo?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Seoul
# SPRING_DATASOURCE_USERNAME=root
# SPRING_DATASOURCE_PASSWORD=change-me

spring.sql.init.mode=never
management.endpoints.web.exposure.include=health,info
